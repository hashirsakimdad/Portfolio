# Hashir Sakimdad — Portfolio

Personal portfolio site for Hashir Sakimdad, AI Engineer.

## Local preview

Open `index.html` in your browser, or serve the folder with any static file server:

```bash
npx serve .
```

## Deploy

This is a static site. It deploys automatically to Vercel when changes are pushed to `main`.
